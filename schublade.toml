# Theme applied as a class on <html> (html.light / html.dark).
catalog = "./catalog.toml"

[server]
host = "0.0.0.0"
port = 47304

[theme]
trigger = "class-name"
key = "theme"
light = "light"
dark = "dark"

[a11y]
enabled = true
rules = ["button-name"]
